# TODO — wrx-api / Domínio das Peças

> Atualizado em 2026-07-22. Itens abertos por prioridade.

## 2026-07-22 - 🟢 Venda CANCELADA → devolve estoque + reativa anúncio (pedido da dona) - ✅ ML NO AR+TESTADO, 🟢 SHOPEE NO AR
- ✅ ML: deployado + testado AO VIVO (order 2000017544550366 SKU 110106: estoque 0→1, 12 anúncios paused→active) + cron minuto 20 LIGADO no GitHub (commit ab74721). Encanamento GitHub Actions→produção validado.
- ✅ SHOPEE (Fase 2, commit a seguir): endpoint `processar-cancelamentos-shopee` (get_order_list CANCELLED + get_order_detail cancel_by) + `reativar_anuncio_shopee` (relist=unlist_item unlist:False) + `_anuncios_unlist_shopee_do_sku_base`. Workflow agora chama os 2 passos (ML+Shopee). Testado observação: order 260713H6KPHUK6 SKU 109349-SH5, `cancel_by=system` ("Seller did not Ship") → classificou "vendedor" → "nada" (CORRETO, regra de segurança OK).
- ⚠️ LIMITAÇÃO SHOPEE: `_anuncios_unlist_shopee_do_sku_base` lê a tabela `shopee_anuncios` (status UNLIST). Se ela estiver incompleta (publish Shopee nem sempre grava — ver TODO antigo), o relist pode não achar o anúncio a reativar. Refinar depois: buscar itens UNLIST direto na API Shopee (get_item_list) por SKU. Faltou testar o caminho ARMADO com um cancelamento Shopee real DO COMPRADOR (não houve na janela).
- Histórico do diagnóstico: a feature já existia mas MORTA (sem cron), não devolvia estoque, buscava SKU exato (vem sufixo), sync só zera. Peça 110106 = importar-web (105; 8765 são PartsHub). Dona: peça PartsHub volta sozinha, importar-web não. Decisões: só se comprador cancelou; ML+Shopee.
- PEDIDO: quando o cliente cancela uma venda, a peça volta pro estoque automático e o anúncio reativa. Print da dona: Moldura Kwid SKU **110106-DML3**, "a pessoa que comprou cancelou porque se arrependeu".
- DECISÕES DA DONA: (1) devolver+reativar **só se quem cancelou foi o COMPRADOR** (se foi a loja/ML, peça pode ter quebrado — só reporta); (2) canais **ML + Shopee** (Shopee = Fase 2).
- 🔎 DIAGNÓSTICO (dados reais): a feature `processar-cancelamentos` (sync_multicanal.py) JÁ EXISTIA mas (a) **nenhum cron a chamava** (morta), (b) foi feita pra NÃO devolver estoque ("PartsHub devolve"), (c) o SKU vem com **sufixo** (110106-DML3) e ela buscava exato → não achava nada, (d) `sync_estoque_qtd.py` só ZERA, nunca sobe. Peça 110106 = `qtd 0`, `origem=importar-web` (NÃO está no PartsHub → nada a devolve, nem manual). 12 anúncios da peça = todos `paused`, nenhum reativou.
- ✅ FEITO (sync_multicanal.py, compila): reescrito `processar-cancelamentos` → busca `orders/{id}` p/ ler cancel_detail, `_quem_cancelou` (comprador/vendedor/desconhecido, conservador), `_sku_base` normaliza sufixo, `_devolver_estoque_local` sobe qtd (0→1, peça única), `_anuncios_paused_do_sku_base` acha pausados por SKU base. modo=observacao devolve cancel_detail cru; armado só age se comprador. + workflow `processar-cancelamentos.yml` (cron minuto 20, ainda NÃO commitado).
- ⏳ FALTA: (1) DEPLOY backend (railway up sobe junto o Flex não-commitado do api_server.py — pedir OK); (2) testar `?modo=observacao&forcar=1` p/ ver cancel_detail real e confirmar heurística "comprador"; (3) `?modo=armado` na 110106 (devolve+reativa os 12); (4) ativar o cron; (5) **peças do PartsHub (89%, origem=null)**: `_devolver_estoque_local` sobe qtd mas o sync horário RE-ZERA em 1h se o PartsHub não tiver devolvido → confirmar se o PartsHub devolve no cancelamento OU proteger no sync; (6) Fase 2 Shopee (detectar CANCELLED + relist).

## 2026-07-21 - 🟢 Desligar Mercado Envios FLEX (em massa + automático < 350) - ✅ DEPLOYADO
- Dona: botão pra desligar Flex em massa na tela Anúncios ML (que ela chama de "mercado turbo") + já desligar todos < R$ 350 + automático (anúncio novo < 350 sai do Flex ao publicar).
- 🔎 RAIZ do que já existia: o endpoint `/integracoes/mercadolivre/flex` estava ERRADO (fazia `PUT items/{id} logistic_type=cross_docking` — "EXPERIMENTAL", NÃO desliga Flex). O frontend `acaoFlexMultipla` já chamava ele (botões "Ativar/Desativar Flex" na barra de seleção), mas com `confirm/alert` nativos e sem funcionar de verdade.
- ✅ ENDPOINT CERTO (conferido na doc + testado AO VIVO): desligar = `DELETE https://api.mercadolibre.com/flex/sites/MLB/items/{id}/v2` → **204** funciona. O `/sites/MLB/shipping/selfservice/items/{id}` (DELETE) dá **403 tengine** (gateway bloqueia) — fica só de fallback. Ligar = `POST /sites/MLB/shipping/selfservice/items/{id}`. Status do item: tags `self_service_in` (ligado) / `self_service_available`+logistic `not_specified` (desligado). DELETE vai SEM Content-Type.
- ✅ BACKEND (api_server.py): `_ml_flex_item` (helper certo, flex_v2 primeiro) + `ml_flex` corrigido + `/flex-massa` (background, `{nome,max_preco,ativar,dry_run,ids}` — escaneia TODOS por preço OU age nos ids; dry_run=default seguro) + `/flex-massa-status` (polling) + `_ml_flex_off_apos_publicar` chamado no `ml_publicar` (thread confere o item em +5/65/245s e desliga se entrou no Flex; limite `_FLEX_OFF_ABAIXO_DE=350`). Removido o `/flex-debug` de diagnóstico.
- ✅ FEITO AO VIVO 21/07: dados reais = só ~10 anúncios estavam no Flex < 350 (as "Mangueira Compressor Corolla", nas 2 contas — quase tudo dela é Agência/Coleta, NÃO Flex). Testei 1 (MLB7135315166 saiu do Flex), depois massa: **default 4 ok + geisa 5 ok = 9, 0 falhas** (10 com o teste). Dry-run depois = 0 restantes.
- ✅ FRONTEND (anuncios-ml.html): botão no topo "⚡ Desligar Flex (massa)" abre modal PRÓPRIO (não confirm/alert) — campo de preço + **Simular** (dry-run mostra quantos) + **Desligar** (real, barra de progresso via polling). Barra de seleção ("Desativar Flex") agora usa o `/flex-massa` com ids + modal. Deploy front (wrangler) + back (railway `-wrx-api`).
- ⚠️ Deploy Railway com rede instável (CLI deu timeout 2x mas o upload/build foi — confirmar pós-deploy pelo endpoint). ⚠️ LIGAR Flex (POST selfservice) pode dar 403 igual o DELETE — não testado (foco é desligar). ⏳ dona: testar o botão + confirmar que anúncio novo < 350 nasce sem Flex.
- ✅ MOTINHA (dona pediu: "símbolo do anúncio que tá com Flex é uma motinha, pra saber qual tá ativo ou não"): no anuncios-ml.html o anúncio no Flex (`logistica==='self_service'`) mostra selo laranja `.selo-flex` "🏍️ Flex" no canto SUP-esq da foto + chip trocou `fa-bolt`→`fa-motorcycle`. Testado AO VIVO com Playwright (eixo Kwid real no Flex mostra a moto).
- ✅ MOTINHA ACESA/APAGADA (dona: "quando sem Flex a motinha fica apagada"): agora TODO card mostra a moto — ACESA (laranja `.selo-flex`) = está no Flex; APAGADA (cinza `.selo-flex.off`) = NÃO está. Só frontend, no ar. Resolve o "não vejo a moto" (os Flex ficavam soterrados: os 8 reais da default são caros — eixos Kwid R$7-9k/bateria/capô, todos >350, corretamente NÃO desligados).
- ✅ FILTRO "Flex" agora BUSCA NO SERVIDOR todos os anúncios no Flex (`carregarFlexDoServidor` → /flex-massa dry_run max_preco alto → amostra) em vez de olhar só os ~50 carregados. Frontend com fallback robusto (normaliza a amostra p/ logistica=self_service) → funciona com backend novo OU antigo.
- ⏳ BACKEND pendente (Railway com rede MUITO instável, `railway up` deu timeout 4x seguidas): a mudança da `amostra` do /flex-massa p/ itens COMPLETOS (cards com foto/medida no filtro Flex) NÃO subiu. Com o fallback do front, o filtro Flex já funciona (mostra título/preço/moto acesa; sem foto/medida até o backend subir). Re-deployar `api_server.py` quando a rede voltar.

## ✅ Feito (2026-07-16)
- **RAIZ do "zera mas não pausa" (overselling) CORRIGIDA e no ar (Railway commit ed7dcb5)**: o passo automático horário `reconciliar-estoque-zerado` (`sync_multicanal.py`, chamado pelo workflow `sync-estoque.yml`) buscava os anúncios da peça vendida por SKU **EXATO** (`sku=in.(base)`), mas 66% dos anúncios são gravados com **sufixo por conta/variação** (`-DML#`/`-GML#`/`-SH#`) → 2 de cada 3 escapavam e ficavam ATIVOS depois da venda. Mesmo bug que já fora corrigido no `finalizar-sku`, `pausar-sku` e webhook Shopee — só ficou de fora do passo automático.
  - PROVA no banco (16/07): 8053 anúncios ML ativos, **120 ML + 168 Shopee (=288) eram de peças com estoque ZERADO**; 118 dos 120 ML tinham sufixo (invisíveis pro match exato). Ex SKU 9317 (Reservatório March) vendido com 12 anúncios ativos; 109776 com 16.
  - FIX: varre TODOS os anúncios ativos e casa pelo **SKU base normalizado** (`re.sub(r'-(SH|DML|GML)\d+$')`). Além disso só pausa o SKU cujo **estoque TOTAL é 0** (`zerados = qtd<=0 MENOS qtd>0`) — protege SKU coringa que ainda tem estoque em outra peça do mesmo código (decisão alinhada com a dona). Salvaguarda anti-massa por teto (`RECONCILIAR_TETO`, default 800).
  - Reversível: `pausar_anuncio_ml` usa `status=paused` (não closed); Shopee = unlist. Dá pra reativar (`reativar_anuncio_ml`).
  - Rodado no ar: observação confirmou 288; armado executado com a dona acompanhando (ver resultado na conversa/memória).
  - LIMITAÇÃO: é a rede de segurança HORÁRIA (cron minuto 5), não instantânea. O pausar-na-hora-da-venda é o webhook ML (`_processar_venda_ml_webhook`), que NUNCA foi confirmado disparando — próxima frente.

## ✅ Feito (2026-07-14)
- **Shopee "Unlist item all failed" RESOLVIDO (backend no ar, Railway commit a1d1dee)**: pausar um anúncio Shopee que JÁ está fora do ar (DELETED/UNLIST/BANNED/em revisão) fazia a Shopee devolver "Unlist item all failed" e a rota `/integracoes/shopee/pausar-item` repassava o erro cru ao card (`api_server.py:12148`). Agora, ao PAUSAR (não ao reativar), se o unlist falha a rota confere o status AO VIVO via `get_item_base_info`: item inexistente ou status≠NORMAL → trata como sucesso e sincroniza o cache (`status: DELETED/UNLIST`). Só no pausar (reativar continua exigindo sucesso real → não mascara overselling). Passa a ler `success_list/failure_list` p/ dar o motivo real. Testado no ar no SKU 5339 (item deletado 58262144598) → `ok=true, ja_fora_do_ar=true`.
  - Diagnóstico do caso: SKU 5339 tinha 2 anúncios Shopee — 1 DELETED (loja Geisa) que dava o erro + 1 NORMAL/ativo (loja Mauricio 234248614, item 58262152989, estoque 1). A Geisa pediu p/ NÃO mexer no ativo, só corrigir o sistema.
  - ⏳ FRONTEND (estoque.html): ajuste cosmético feito (toast "já estava fora do ar") mas NÃO deployado — o wrangler sobe a pasta inteira e ela ainda tem o gatilho `salvarQtdRapido→_pausarAnunciosPorBaixa→/pausar-sku` SEM trava anti-coringa (não pode ir pro ar; ver bug_venda_nao_encerra_anuncios). Subir só junto com a trava.

## 🔐 SEGURANÇA — rodada pendente (combinado com a usuária 2026-06-06)
- Mover SEGREDOS hardcoded no código → variáveis de ambiente (Railway) + ROTACIONAR os expostos:
  - sync_partshub.py: PH_EMAIL/PH_SENHA (senha "Vitoria12$" em texto), PH_ANON, SB_KEY.
  - sync_multicanal.py / api_server.py: chaves Shopee (partner key), Supabase service/anon, tokens.
  - Frontend (netlify): Supabase publishable key é anon (ok público), mas conferir se não há service key.
  - Regerar a SENHA do PartsHub depois de mover (ela foi vista no repo).
- ✅ MARCA/MODELO no estoque (2026-06-06): PartsHub guarda marca/modelo em compatible_vehicles[].brandName/modelName (vehicle_brand vem VAZIO). Corrigido sync_partshub (_cv_brand_model) → 2540+ peças c/ marca (Nissan/Kicks, Peugeot/Expert...). Fix PGRST102 (tirou NCM/CEST do upsert — chaves variaveis). Filtros Marca/Modelo na lateral aparecem sozinhos quando há dado. Re-rodando sync p/ atualizar todas.

## 🟢 EM ANDAMENTO (2026-06-07)
- ✅ **REVISÃO DE PREÇOS (FUNCIONANDO, no ar 2026-06-07)**: página /revisao-precos (grade Foto|Peça|Meu|Menor|Média|Sugestão|Diferença|Prioridade|Ação; Aprovar/Editar/Ignorar; Limpar lista). Tabela revisao_precos criada (usuária rodou o SQL). Aprovar aplica preço no ML (PUT user token).
  - RASPAGEM = LOCAL NO PC (de graça): a API do ML é 403 e o Railway toma captcha → a busca roda no PC via **`revisao_precos_local.py`** / atalho **"Revisao de Precos.bat"** (receita anti-bloqueio do _raspar_ml). Manda pro endpoint /revisao-precos/salvar-item (filtra+calcula+grava). Usuária ESCOLHEU PC (Railway = caro/pesado).
  - Filtro relevância = PALAVRA-CABEÇA (porta ≠ maçaneta da porta) + corte de outlier (0.6x-1.7x mediana) + tira vendedor>2. VALIDADO real (Intercooler R$399 vs mercado ~R$900).
  - LIMITES: prioriza só "mais antigos (SKU) + com estoque" (ml_anuncios não tem data/venda). Título com erro de digitação (ex "Renalt") → ML não acha (0 preços). Ver memória [[revisao-precos-modulo]].
  - BUSCADOR COMPLETO (2026-06-07): modo "tudo", trata captcha (espera+reaquece+retry), pausas, pula o que já está na lista, pula vendido (qtd PartsHub). Título na tela → abre /estoque?sku (apagar velho/criar novo).
  - USUÁRIA ESCOLHEU RODAR NA MÃO (não agendar). Atalho "Revisao de Precos.bat" → digita "tudo" ou número.
- 🔜 ML anti-duplicado (usuária disse "sim"): não republicar se o SKU já tem anúncio ativo no ML (caso dos 24 anúncios no 109092). PENDENTE.

## 🟢 EM ANDAMENTO (2026-06-06 noite)
- ✅ SELECAO estilo PartsHub: quadradinho .sel-check SEMPRE visivel no card (era gated por modoSelecao) — clicar card abre, clicar quadradinho seleciona → barra de lote (#lote-bar atualizarBarraLote, abre c/ selPecas>0): Editar massa/Imprimir/Atualizar dimensoes/Sincronizar mkt/Excluir. Testado 25 checks + barra abre.
- ✅ SCANNER QR/codigo na BUSCA do estoque: botao "Escanear" + abrirScannerBusca (BarcodeDetector camera; fallback prompt). Le QR/barras → _extrairCodigoScan (pega sku de etiqueta.html?sku= ou raw) → busca-input + filtrarPecas. Testado funcao+botao.
- 🔜 PEDIDO: pausar/finalizar/EDITAR QUANTIDADE direto no CARD do estoque (hoje so em peca→Rastreamento→Marketplaces). Confirmar e fazer.
- ✅ Categoria DO PartsHub: confirmado puxa (sync_partshub cat→pecas_estoque.categoria; frontend mostra).
- ✅ VIOLACAO SHOPEE = itens de SEGURANCA vendidos como USADO. A Shopee proibe usado em: AIR BAG, CINTO seguranca, DIRECAO (caixa/coluna/terminal/barra axial/braco/pitman), FREIO (pastilha/disco/tambor/sapata/cilindro/pinca/servo/ABS/flexivel), SUSPENSAO (amortecedor/mola/bieleta/bandeja/pivo/braco oscilante/feixe mola/mola ar), VIDROS de seguranca (parabrisa/vidro traseiro-lateral-teto). EXPANDI itemRestritoComotUsado + ITENS_SOMENTE_NOVO (dx-criar-core) p/ cobrir TUDO; excecoes p/ MAQUINA/MOTOR de vidro+borracha/moldura/friso (mecanismo NAO e o vidro). Fix cinto sem cedilha. Ao publicar Shopee usado desse grupo → forca NOVO + avisa. Testado 26 casos. dx-criar-core?v=8. (A API Shopee NAO expoe a lista de violacoes — so Seller Centre.)
- 🔜 RAIO COMPATIBILIDADE ML (planejado, NAO depende de OEM): a compatibilidade com IDs do ML JA vem do PartsHub (compatible_vehicles tem mlYearId/mlBrandId/mlModelId). Plano: criar-anuncio puxa do PartsHub (dedup — vem repetido, ex SKU 109039 tinha 2025 3x+2026, faltava 2024) + lista EDITAVEL (add/remove ano/veiculo) + no publicar envia ESTRUTURADO (IDs ML) → ficha oficial "Compativel com". OBS: dado do PartsHub tb falha (faltam anos), por isso editar e essencial.
- ✅ SHOPEE /violacoes (commit 5b4ba8b): endpoint lista produtos BANNED (em violacao) das 2 lojas c/ nome+SKU. MOTIVO de cada um a API NAO devolve — so o Seller Centre (Produtos→Violacao→clicar→motivo+Recorrer). Falta: botao no painel/estoque pra ver a lista (hoje so via endpoint).

## 🔴 Pendentes / aguardando ação
- [x] ~~**RECONSTRUIR criar-anúncio a partir do mockup/foto (v5)**~~ → **FEITO e NO AR (2026-06-05)**: criar-anuncio-v5.html construída no molde da foto (2 páginas), usuária aprovou ("isso"), sistema repontado pra v5 e deploy feito. Ver seção "Feito (2026-06-05)". Deploy correto = `netlify deploy --prod --no-build --dir=.` (NÃO precisa unlink/relink — o que falhava era a etapa de build).
- [ ] **Painel não puxa anúncios ANTIGOS (pedido 2026-06-05)**: SKUs já anunciados antes do fix (ex 109044 = 3 anúncios ativos no ML) não aparecem no card porque nunca foram gravados no ml_anuncios (o publish só grava os NOVOS). SOLUÇÃO: rodar/criar um SYNC que puxa todos os anúncios ATIVOS do ML (via _ml_buscar_todos_ids scan, 2 contas) → upsert em ml_anuncios (SKU base). Idem Shopee → shopee_anuncios. Depois disso TODOS os cards refletem.
- [ ] **Botão de PAUSA no card (pedido 2026-06-05)**: card do estoque/anúncios precisa de botão pausar (e reativar). Backend já tem endpoint de pausar (ml-pausar / sync_multicanal pausar_anuncio). Falta o botão na UI chamando por ml_id+conta.
- [ ] **Foto no tamanho MÁXIMO do ML** (pedido 2026-06-05): a foto editada vem do canvas (`canvasFotoLimpaDataURL`) — se o canvas for pequeno, publica foto pequena. Garantir que o export saia em alta (ML recomenda ≥1200px, ótimo ~2000px no maior lado) pra não ficar foto pequena. Programar o tamanho certo na geração do base64.
- [ ] **Card do produto reflete publicação** (pedido 2026-06-05): conferir/ligar que o card do estoque (estoque.html) atualize "Sem anúncio" → "Publicado" quando o anúncio entra (lê ml_anuncios; depende de sync ou de o publish gravar).
- [ ] **Redesign criar-anúncio em 2 páginas** (visual DP Peças, sem rolar). Mockup visual APROVADO pela usuária em `netlify-deploy/criar-anuncio-mockup.html` (foto 1º, marcadores, carrinho de compat por silueta, campo "cole título/código→puxa tudo", raio categoria ML, catálogo auto ML, SKU dianteiro/traseiro+lado, NCM/CEST, contador de caracteres, resumo com Postados/Falhas). Spec títulos: **ML 2 contas × 5 (60 car.)**, **Shopee 2 contas × 5 (90 car.)** + Site + OLX, checkbox por título.
  - REGRA DA USUÁRIA: tudo em **BLOCOS SEPARADOS** (nunca monolito). E quer **IGUAL à apresentação** (mockup), tudo.
  - SPEC TÍTULOS FECHADA (2026-06-05): cada conta **ML1, ML2, Shopee1, Shopee2 = 5 anúncios**, e **CADA anúncio tem título + preço + condição(novo/usado) + checkbox** EDITÁVEIS no painel (ML 60 car, Shopee 90 car). + Site + OLX (1 cada). Substitui o modelo atual de "4 slots de preço compartilhados".
  - PROGRESSO v3 (`criar-anuncio-v3.html`): já tem sidebar DP Peças + tema escuro alinhado + 2 páginas + paginação, lógica 100% ligada. FALTA: (estágio 1) montar a nova UI de títulos por conta na Pág 2 com campos NOVOS (manter os antigos `mp-t-*` escondidos p/ não quebrar publish); (estágio 2) religar planosMercadoLivre/Shopee p/ ler os campos novos + TESTAR publicar de verdade; (3) deixar Pág 1 idêntica ao mockup (3 colunas); (4) trocar criar-anuncio.html pela v3.
  - RISCO: o estágio 2 mexe na lógica que VENDE (planosMercadoLivre em dx-mercadolivre.js: hoje 4 modelos×2 contas; mp-t-1..4 + mp-p-*). Fazer com cuidado, na v3, testar antes de ir pro ar.
  - FEITO 2026-06-04: extraído o inline (5763 linhas) p/ bloco `dx-criar-core.js` → `criar-anuncio.html` agora 1101 linhas; verificado no preview (todas funções OK, 0 erro). Logica já é reusável.
  - FALTA: (a) afinar `dx-criar-core.js` em blocos menores por responsabilidade — **fotos/editor, compatibilidade, títulos, categoria/NCM-CEST, busca/OEM, cadastro/core** (são 272 funções, ver nomes no core); (b) montar a v2 incluindo os blocos + DOM com os MESMOS ids (f-sku, mp-t-1..4, sug-txt-*, ph-input, etc.) pras funções acharem; (c) testar publicar de verdade = igual; (d) trocar o link.
- [ ] **Shopee tax_info** — preencher `Downloads/mass_update_tax_info_1545866669_*.xlsx` (loja 1545866669); dados fiscais (NCM/CEST) faltando da conta. (pedido pela usuária 2026-06-04, "depois")
- [ ] **Promover modularização do criar-anúncio pra PROD** — preview testado (carrega, funções OK, status ML corrigido). Falta o teste de publicar de verdade = igual, depois `netlify deploy --prod`.
- [ ] **Domínio curto `dominiodaspecas.com.br` (SSL)** — DNS certo (A→75.2.60.5), site OK em http. HTTPS travado: Netlify preso emitindo cert do `...catonho` (domínio MORTO, sem DNS, expirando). Destrava só no painel web (Set primary no curto + Remove catonho) quando o cert do catonho falhar. NÃO renovar o catonho. API recusa (Unprocessable). Ver [[deploy-site-oficial-netlife]].
- [ ] **Webhook venda ML instantâneo** — código no ar; usuária cadastrou callback (corrigido de oauth/callback p/ /webhook + orders_v2). CONFIRMAR na próxima venda real (entrada nova no `sync_log` sem simulação).

## 🟡 Bugs/observações encontrados (2026-06-04)
- **Status ML não aparecia no card** após publicar: backend `/status-sku` retorna `ok`+`anuncios` mas não `encontrado`; frontend checava `data.encontrado`. CORRIGIDO em `dx-mercadolivre.js` (passa a olhar `anuncios.length`). No preview; falta ir pra prod.
- **Shopee publish não grava `shopee_anuncios`**: `publicarShopeeReal` publica mas não registra no cache → painel não mostra "enviado pra Shopee". Cache é populado por sync separado. Avaliar gravar no publish.
- **Gerar Títulos IA (sug-txt 0-3)** veio vazio (caiu no título base) — código de títulos ainda inline; investigar à parte.
- **Qtd inflada** (peça única com qtd>1, ex 8109=3): cega o anti-venda-dupla; precisa critério da usuária p/ filtrar no PartsHub.
- **2683 peças em estoque SEM localização** no PartsHub (cadastro da equipe, não é bug do sync).

## 🔴 FILA ESTOQUE (Netlify LIBERADO 2026-06-06; deploys voltaram)
- ✅ LOTE A no ar: nav "Dashboard"→painel.html (criar-anuncio-v5); filtro "SEM anúncio OLX/ML/Shopee" add (f-integracao + logica). DESCOBERTA: filtros com/sem localização (f-local-status) e ML/Shopee/OLX (f-integracao) JÁ existiam. marketplacePeca só detecta 'ml' com clareza (shopee por outro caminho) — filtro por CONTA (ML Geisa/Domínio, Shopee Mauricio/Geisa) ainda pendente.
- ✅ LOTE B no ar: etiqueta da peça (abrirEtiqueta) agora com QR REAL (api.qrserver.com) encodando https://dominiodaspecas.com.br/estoque.html?sku=SKU (?sku já abre a peça). Testado.
- ✅ META EQUIPE (10) no ar: bloco dx-meta-equipe.js (incluído em criar-anuncio-v5). Cartela no topo: "[nome] meta hoje X/Y faltam Z" + barra; clica→define meta (default 60); conta +1 ao publicar (envolve publicarTudoNovo/Usado/Tudo). Testado (0/60→1/60). LIMITAÇÃO: contagem localStorage por navegador (cross-device precisa backend); config é meta geral (por-pessoa + admin agregado = painel.html ranking, separado).
- ✅ MENU ML/SHOPEE/OLX no ar: agora abrem o estoque ja FILTRADO por marketplace (estoque.html?integracao=ml|sh|olx). Testado: ML 845 cards, Shopee 132. Correcoes: marketplacePeca passou a enxergar Shopee (window._shopeeSkuCache; antes so anunciosPartHub=ML); carregarCacheShopee repopula filtros (aparece opcao "Shopee"); filtro via URL re-aplica varias vezes (select e repopulado no load). OLX nao filtra (nao ha anuncio OLX, publicacao bloqueada). Bonus: filtro "SEM anuncio na Shopee" agora funciona.
- ✅ QR ETIQUETAS EM MASSA no ar: loteImprimirEtiquetas agora gera QR real (api.qrserver.com, link estoque.html?sku=) ao lado de cada etiqueta (layout flex qr+info). NAO tinha typo <\div> (estava ok). 
- ✅ RENAME "Não endereçada"→"Não localizado" no ar: trocado nos 3 pontos visiveis (card grid 19301, etiqueta lote 19584, etiqueta individual 20258). NAO troquei os ~3000 dados do pecasMock (sao fallback; dado real vem do backend) — a troca e no display. Confirmado 0 ocorrencia visivel de "Não endereçada".
- ✅ FILTRO POR CONTA no ar: opcoes add no f-integracao — "ML · Dominio" (ml_default=778), "ML · Geisa" (ml_geisa=144), "Shopee · Geisa" (sh_1545866669=131), "Shopee · Mauricio" (sh_234248614=1). Helper contaMatchPeca (ML via integrationId, Shopee via shop_id no _shopeeSkuCache). MAPA CONFIRMADO pela usuaria: ML default=Dominio, geisa=Geisa; Shopee 1545866669=Geisa (Auto Pecas Geisa Catonho, 57 ativos no painel Shopee), 234248614=Mauricio.
- ✅ STATUS FILTERS confirmados funcionando (SO ML): Publicado 817, Pausado 30, Encerrado 0, Sem anuncio 5341. LIMITACAO: status le so anunciosPartHub (ML); Shopee (NORMAL/UNLIST no _shopeeSkuCache) NAO entra na contagem de status. Estender p/ Shopee = pendente.
- 🔜 PEDIDO 2026-06-06 (PESADO, mexe em anuncio real): pausar/finalizar/editar QUANTIDADE direto no card SEM sair da pagina. Backend JA TEM: ML /integracoes/mercadolivre/pausar-item (status paused|active|closed = pausar/reativar/finalizar), Shopee /integracoes/shopee/pausar-item (unlist) + /integracoes/shopee/atualizar-estoque (qtd). Card ja tem botao "Pausar" mas e FAKE (pausarAnuncio linha 20280 so mostra toast, nao chama backend). FALTA: wire real (1 peca pode ter varios anuncios: 2 ML Dominio + 1 Geisa + Shopee → agir em todos) + campo qtd inline + ML qtd update (add endpoint PUT available_quantity). RISCO: testar = pausar/finalizar anuncio REAL.
- 🔜 PEDIDO 2026-06-06: diretrizes produto proibido Shopee — RESPONDIDO (pesquisa). Maquina vidro eletrico NAO e proibida; violacao provavel = categoria/marca/GTIN invalido/titulo. Motivo exato so no Seller Centre aba Violacao. Possivel feature futura: aviso de palavra-proibida no publish Shopee.
- ✅ TÍTULOS POR CONTA + DESCRIÇÃO (pedido 2026-06-06, criar-anuncio-v5):
  - IA gera 5 títulos VARIADOS ate 90 car (backend /gerar-titulos prompt novo + cap [:90]; era 4 e [:60]). 1 REQUISICAO so (nao 5). Guardados no array global `titulos` (completos), nao truncados pelo sug-txt(60).
  - renderTitulosPorConta: fonte = titulos[idx] (IA, full) → ML corta 60, Shopee mantem 90. Os MESMOS 5 titulos se repetem nas 4 contas (linha i = titulo i em todas).
  - CONTA MESTRE = ML Geisa (tc-ml1): editar uma linha REPLICA nas outras contas (_tcPropagar; ml2/sh1/sh2 mesma linha; site/olx so linha 1; corta no limite de cada canal). Outras contas continuam editaveis (override). TESTADO no ar.
  - DESCRIÇÃO (montarDescricaoWRX agora e a fonte da verdade): CÓDIGO OEM no topo + COMPATIBILIDADE "• Marca Modelo MOTOR Ano" um por ano (motor de v.detalhes; fallback extrai 1.6/2.0/turbo do nome da peca). TESTADO. Cache-buster dx-titulos-conta.js?v=4, dx-criar-core.js?v=4. Backend commit 366ba2b (Railway subindo).
- ✅ STATUS REAL + PAUSAR/FINALIZAR + RASTREAMENTO (pedido 2026-06-06, estoque.html):
  - DIAGNOSTICO: cache ml_anuncios estava FURADO — SKU 109032 mostrava "10 pausados" (MLB4741, listings velhos de republicacoes) mas o ML REAL tinha 4 ATIVOS (MLB6862). Cache guardava os mortos e nao pegou os vivos.
  - RESYNC completa rodada (/sincronizar-anuncios): 1440 SKUs ativos, 3523 anuncios. Sync faz UPSERT (nao apaga mortos) — por isso os pausados velhos ainda aparecem; usuaria quer "so puxar ativos, pausados/finalizados na aba propria" (filtro de status ja separa: Publicado/Pausado/Encerrado).
  - Aba Marketplaces do RASTREAMENTO agora tem: botao "Verificar status REAL no ML (ao vivo)" (chama /status-sku, busca por seller_sku, mostra verdade) + por anuncio ML: Reativar/Pausar/Finalizar (acaoAnuncioML → /pausar-item status active/paused/closed). TESTADO ao vivo (4 ativos retornados). Acao real nao testada (mexe em anuncio real) — usuaria testa.
  - RASTREAMENTO "a partir de hoje": registrarRastreamento (localStorage dx_rastreamento_v1) registra quem+quando+o que; operador definido 1x por dispositivo (dx_operador_v1, prompt). getAudit mescla o log no historico. salvarEdicaoLocalEstoque registra edicoes. Acoes pausar/finalizar/reativar tambem entram. TESTADO. LIMITACAO: localStorage por dispositivo — centralizar (ver todos funcionarios) precisa tabela backend.
- ✅ SISTEMA.HTML menu re-apontado (2026-06-06): era casca c/ maioria "Em desenvolvimento". Re-apontei p/ paginas reais: Dashboard/Vendas/Lucratividade/Vendedores→painel.html, Clientes→crm.html, NCM e CEST→configuracoes-dados.html, Etiquetas→estoque.html. Continuam "Em desenvolvimento" (nao existem): Comissoes, Fiscal NFe/CFOP, Empresas, Usuarios, Fornecedores, Categorias ML, Tutoriais, Suporte.
- 🔍 ANUNCIO NAO VINCULADO (investigado 2026-06-06): CAUSA RAIZ = seller_sku VAZIO em TODOS os anuncios ML (campo oficial nao preenchido). Sistema adivinha SKU pelo titulo (_ml_extrair_sku); funciona em ~9/10 (testado: 2123/2393/8712 existem no estoque) mas falha qdo titulo nao tem SKU ou SKU nao esta no estoque (ex 1000). Criado endpoint SOMENTE-LEITURA /integracoes/mercadolivre/relatorio-vinculo (commit fc13f92) p/ contar vinculados vs nao + amostra. CORRECAO PROPOSTA (aguarda OK p/ escrever no ML): rotina que grava seller_sku=SKU casado em cada anuncio ativo (vinculo exato/permanente) + lista os sem-match p/ vincular na mao. ATENCAO: escreve em anuncio real.
- ✅ CRIAR-ANUNCIO fixes (2026-06-06): (a) edicao NAO some ao mudar de pagina — renderTitulosPorConta(forcar) preserva valores editados (snapshot _prev); botoes Gerar Titulos passam forcar=true. (b) descricao NAO inventa mais compat — data.versoes SEMPRE = compatData (carros curados do painel), nao os da IA isolada; rebuildDescricao() reconstroi de compatData ao entrar na pag2 (irPagina(2)). (c) botao IA na pag1 (gerarTituloIApagina1). Cache dx-criar-core?v=6, dx-titulos-conta?v=5. Testado funcoes intactas.
- 🔜 FILA ABERTA (reportada por ela testando, 2026-06-06) — pendente, escolher prioridade:
  1. criar-anuncio: titulo SHOPEE = OLX = Site proprio (mesmo titulo nesses 3 canais; hoje master ML Geisa ja propaga p/ todos linha 1).
  2. ✅ LOGIN DE FUNCIONARIO no ar: bloco dx-login-funcionario.js — lista a equipe (Supabase funcionarios, mesma fonte do painel: Adriano/Bia/geisa/Giane/Rafael/Rayane...), pessoa escolhe → grava dx_usuario_atual (meta usa) + dx_operador_v1 (rastreamento usa). Badge no topo p/ trocar. 1a vez abre sozinho. Incluido em criar-anuncio-v5 + estoque. TESTADO: escolheu Bia → cartela "Bia meta hoje 0/60", usuarioAtualCadastro=Bia, operador=Bia. Unifica meta + rastreamento. (Painel tb poderia ter, futuro.)
  3. PAINEL.html travado em "Carregando..." (Mensagens recentes/Equipe/Faturamento/Resumo/Pedidos hoje/Mensagens hoje as vezes nao carregam) + quer mostrar VENDA DO DIA (hoje so mostra venda do mes).
  4. EXPEDICAO nao atualiza (expedicao & separacao — botao Atualizar / dados nao refrescam).
  5. SHOPEE mostrando PEDIDO ERRADO (expedicao/painel — pedido shopee incorreto).
  6. "peca na frente" no editor — esclarecer (ja existe botao "Carro atras" = peca na frente dos carros).
- ✅ BLOCO ETIQUETA (2026-06-06): (1) bug 71 folhas RESOLVIDO — era visibility:hidden q nao colapsa layout (estoque inteiro = 71 pgs invisiveis); agora imprimirEtiquetaLimpa(id) abre janela nova SO com a etiqueta. (2) QR NAO abre mais o sistema — aponta p/ NOVA pagina etiqueta.html (minima, read-only: SKU+loc+foto via params, sem menu/fetch/sistema) via _urlEtiquetaMinima(p); testado temMenuSistema=false. (3) nome "Domínio das Peças" REMOVIDO da etiqueta (2 contas) — modal header + templates. (4) lote tb usa _urlEtiquetaMinima. PENDENTE MENOR: parts c/ loc "Nao enderecada" (sem acento, vem de normalizarPecaEstoque) mostram isso no QR em vez de "Não localizado" — check so pega "Não endereçada" c/ acento; folar no proximo deploy (helper _locLabel).
- ✅ (6) PAINEL no ar: travava pq (a) linha 245 caia em mlV/shV INDEFINIDOS (ReferenceError) qdo busca 180d falhava — corrigido p/ []; (b) await da busca 180d (lenta) BLOQUEAVA pedidos/msgs/equipe — agora faturamento roda em IIFE fire-and-forget (nao trava). + VENDA DO DIA: card "Vendas do mes" mostra "Hoje: R$X" (vendaDia=somaV(hojeF ML+SH)). Testado: equipe/msgs/pedidos/kpis carregam na hora; faturamento preenche depois.
- ✅ (4) EXPEDICAO atualiza: aba Conferencia/Separar (carregarASeparar) + botao Atualizar agora chamam sincronizarFila em 2o plano (importa ml-a-enviar+shopee-a-enviar). Antes SO a aba "A Enviar" sincronizava → Separar nunca pegava pedido novo. Flag _aSepararSyncing evita loop; comSync=false na re-render. Testado: syncChamou ml+shopee.
- ✅ (5) SHOPEE pedido errado: DIAGNOSTICADO via novo ?debug=1 em shopee-a-enviar (sync_multicanal.py) — ambas lojas token OK, 0 READY_TO_SHIP/PROCESSED, sem erro. NAO ha pedido real escondido. Fantasma = lixo antigo na fila q nunca limpou (limpeza so rodava se loja devolvia >=1). FIX: expedicao.html sincronizarFila usa shopee-a-enviar?debug=1 → shopsOkShopee (token+sem erro) autoriza marcar enviado os fantasmas mesmo com 0. Commit eb91d35 (backend) + frontend deploy.
- ✅ TITULO SHOPEE=OLX=SITE: editar Shopee (tc-sh1) replica em tc-sh2+site+olx (_tcPropagarShopee); ML Geisa segue mestre do grupo ML. Testado. dx-titulos-conta.js?v=7.
- ✅ "TA NO ML MAS NAO NO SISTEMA" / contagem errada RESOLVIDO: anuncios-db lia ml_anuncios com limit=5000 mas PostgREST corta em 1000 → so os 1000 + recentes vinham, ~3597 anuncios (4597 total) sumiam → SKUs apareciam "Sem anuncio" (ex 109010/109040 ativos no ML). FIX: paginar anuncios-db (1000 em 1000). Commit a7e193c. MESMO bug do skus_sistema e do report-vinculo (PostgREST 1000-cap — padrao a vigiar em TODA query Supabase grande).
- 🔜 DEPOIS (pedido, ver mais tarde): contagem "Publicado 907" no estoque NAO bate com ~1475 ativos no ML; quer pagina que ATUALIZE SOZINHA + sync automatico pela API (ml_anuncios fresco, contagem real). Tem /sincronizar-anuncios; falta auto-rodar + refletir contagem (modelo: Gestao Desmonte tem sync ao vivo c/ progresso).
- ✅ FILTRO estoque lateral (estilo Gestao Desmonte): mostrei na lateral os selects que estavam hidden (f-cat/f-sub/f-marca/f-modelo/f-versao) com rotulos; populei f-versao; ESCONDI a barra horizontal "Filtros Avancados" (#filtros-avancados-card display:none, elementos mantidos p/ JS nao quebrar); galpao/setor seguem hidden. ACHADO: dados PartsHub so tem CATEGORIA (6186/6186); marca/modelo/sub/versao VAZIOS (0/6186) → escondo filtro vazio automaticamente (só aparece se houver dado). Categoria(374)+Condicao funcionam, testado filtrar. Pra ter marca/modelo precisa enriquecer o DADO da peca (catalogo/OEM). | criar-anuncio: edicao AINDA some ao mudar pagina (verificar foto/carro, nao so titulo) + botao pausar/finalizar NA tela de edicao + "muda de pagina em vez de rolar pra baixo" + (7)titulo shopee=olx=site (8)peca na frente.
- FALTA (maiores): etiqueta de CAIXA/localização + escanear câmera (mobile, pesado). Menores: rastreamento CENTRALIZADO (backend, p/ dona ver todos); sync que LIMPA mortos (ou display só-ativos); status filter incluir Shopee; meta no backend; qtd inline.

- **Netlify deploy BLOQUEADO**: erro 403 "Account credit usage exceeded - new deploys are blocked until credits are added". Usuária vai liberar (billing/crédito ou reset). Apaguei 9 sites netlify sobrando (sobrou só charming-marzipan=dominiodaspecas.com.br). LIÇÃO: juntar mudanças e fazer 1 deploy só (cada deploy consome crédito).
- **PRONTO p/ subir (já no arquivo, falta deploy)**: (1) estoque modoSelecao=false → clicar no card ABRE pra editar (não seleciona); seleção em massa = visão LISTA (checkboxes sempre). (2) badge verde/vermelho "Etiqueta impressa/não impressa" no card + loteImprimirEtiquetas marca etiquetaImpressa=true. (Medidas por categoria já subiu antes do bloqueio.)
- **A CONSTRUIR (depois de validar 1-3)**: (4) filtro COM/SEM localização (achar 2683 sem local); (5) filtro lateral por marketplace+conta (ML conta1/2, Shopee loja1/2) **incluindo "SEM anúncio na OLX"**; (6) etiqueta da peça com QR REAL = link abre a peça (dominiodaspecas.com.br/estoque.html?sku=) + dados SKU/local no texto; (7) etiqueta de LOCALIZAÇÃO (ex "Caixa 1 · Prateleira 3") com QR + FLUXO: escaneia a etiqueta da caixa → local fica ativo → vai adicionando produtos naquela caixa (feature mobile/almoxarifado, mais pesada). Decisão usuária: QR da peça = link+dados; etiqueta local = colar na caixa/prateleira e escanear p/ ir colocando produtos.
- **META DIÁRIA DA EQUIPE** (10): (a) painel/config onde a admin DEFINE a meta diária por pessoa (ex 60 anúncios/dia); (b) CARTELA na tela criar-anuncio mostrando a meta do usuário logado em tempo real (ex "Bia 1/60 · faltam 59"); (c) CONTAGEM real de anúncios por pessoa/dia (backend agrega por cadastradoPor+data). Base já existe: painel.html tem "Ranking de meta de anúncios da equipe". Sistema registra cadastradoPor/usuarioCadastro no publish.
- **NAV/MENU quebrado**: (8) menu "Dashboard" aponta p/ dashboard-vendas.html — apontar p/ **painel.html** (o dashboard dark real que a usuária procurava: Painel Administrativo, marketplace por conta, meta anúncios, ranking, equipe; está no ar em /painel.html). (9) menu "Mercado Livre"/"Shopee"/"OLX" vão todos p/ marketplaces.html e "não funcionam" → arrumar (apontar p/ página certa OU filtrar estoque por aquele marketplace).

## ✅ Feito (2026-06-06)
- **Estoque: edição em massa igual PartsHub + medidas por categoria**: (1) `modoSelecao=true` por padrão → checkbox SEMPRE visível nos cards (antes precisava clicar "Selecionar" 1º; era o "barra não aparece"); barra com 5 botões (Editar massa/Imprimir/Atualizar dimensões/Sincronizar mkt/Excluir) já existia. (2) NOVO: botão "Medidas/categoria" → modal lista TODAS as 373 categorias distintas (de `pecasMock`, campo `cat`, folha) c/ inputs Comp/Larg/Alt/Peso, salva em localStorage `dx_medidas_categoria_v1`. (3) "Atualizar dimensões" agora chama `loteAtualizarDimensoesCategoria()` → aplica a medida da CATEGORIA de cada peça selecionada (igual PartsHub "com base na categoria"); usa `salvarEdicaoLocalEstoque` (edição local). LIMITAÇÕES: medidas no localStorage (por navegador); atualização é local no painel (refletir no mkt = botão Sincronizar). TESTADO no ar (373 cats no modal, checkbox sempre visível). Deploy netlify feito.
- **DESCOBERTA "dois painéis"**: NÃO são dois — charming-marzipan.netlify.app = dominiodaspecas.com.br (md5 idêntico). catonho domain está fora (HTTP 000/SSL). Conta tem 10 sites netlify (sobras). Caches estáticos partshub-*-cache.json 404 mas são inofensivos (estoque busca anúncios ao vivo em /integracoes/mercadolivre/anuncios-db, que funciona). "marketplaces sumiu" era cache.

## ✅ Feito (2026-06-05)
- **Remover fundo via Pixian.ai — DEPLOYADO (2026-06-05)**: backend push main `a15f6c4→a971571` (Railway -wrx-api sobe sozinho); frontend netlify --prod OK (6 arquivos, charming-marzipan). Usuária pôs PIXIAN_API_ID/SECRET + PIXIAN_TEST=true no Railway (serviço -wrx-api). FALTA: testar no ar; se qualidade ok, REMOVER PIXIAN_TEST (passa a cobrar/qualidade cheia); usuária deve REGERAR a chave Pixian (secret foi exposto em print). Detalhes abaixo ↓
- **Remover fundo via Pixian.ai (API paga) — código pronto, falta credencial/deploy**: backend `/remover-fundo` ([api_server.py:2026](api_server.py)) agora tenta PIXIAN PRIMEIRO (env `PIXIAN_API_ID`/`PIXIAN_API_SECRET`; opcional `PIXIAN_TEST=true`), retorna na hora se OK, senão cai no rembg/GrabCut. Frontend (dx-criar-core `removerFundoAPI` + dx-carros-modal `_removerFundoDeUrl`): REMOVIDO localhost:5678 (não depende mais do PC) e REDUZ p/ 1200px no maior lado antes de enviar (economiza no Pixian — modelo por megapixel). Custo Pixian ~$0,02/credito (25MPx=1cred); 1000 fotos ~1200px ≈ R$8–16 (dólar R$5,17). py_compile OK; funções verificadas no browser. FALTA AÇÃO USUÁRIA: criar conta pixian.ai → pegar API ID+Secret → pôr no Railway (env) → deploy backend (Railway) + frontend (netlify --prod --no-build --dir=.). Comparação: no volume 500+, Photoroom seria mais barato em foto grande, mas com redução p/ 1200px o Pixian fica barato e simples (escolha da usuária). Cache: dx-criar-core.js?v=2, dx-carros-modal.js?v=2.
- **Modal de carros compatíveis (novo, dx-carros-modal.js)**: botão "Carros compatíveis" (no lugar do painel fixo embaixo, economiza espaço) → abre MODAL com os carros da peça (compatData + título), fotos no ângulo **frontal 45° esquerda** (`_fotoCarroFrontal45`, query Bing), **multi-seleção**. Ao confirmar: cada carro → **remove fundo** (`_removerFundoDeUrl`: localhost:5678 rembg → fallback Railway GrabCut) → **salva no banco** (salvarCarroBanco) → **vai pro canvas** (toggleCarro, movível). Se rembg falhar, põe a foto original (não trava). Reusa funções do core. Grid antigo #cars-panel escondido (ids mantidos). Include após dx-criar-core.js. TESTADO UI (modal abre, 12 carros, multi-seleção, URL 45° ok). FALTA TESTE REAL: a remoção de fundo precisa do backend (não dá p/ validar offline) + deploy. Obs: bug antigo "carro não aparecia/linha branca" era o painel inline instável — substituído pelo modal.
- **Responsivo celular (v5)**: media queries reescritas — `@media(max-width:1100px)` força FLUXO NATURAL (html/body/main/content/page height:auto+overflow visível; .p1/.p2/#editor-grid viram flex-column; #titulos-por-conta 1 coluna; #foto-canvas width:100% height:auto max 520px; topbar/footbar quebram linha). `@media(max-width:860px)` esconde sidebar + canvas 100% + botões do rodapé flex. BUG CORRIGIDO: canvas sumia no empilhamento (alturas 100vh/flex:1 colapsavam p/ 0). TESTADO em 390px (pág1: canvas aparece, tudo empilha; pág2: títulos 1 coluna, total 22, rodapé quebra). Obs: widget flutuante "AO VIVO" (global) sobrepõe um pouco — não é desta página.
- **Lote movido p/ topo + validação de dimensão ML**: (1) botão **Lote** agora no topo ao lado de "Categoria ML"; **Lote/Catálogo removidos** da lateral (card Compatibilidade só tem "Busca automática"). (2) **Validação de dimensão do Mercado Envios** — `validarDimensoesMercadoLivre()` (dx-mercadolivre.js) chamada no início de `publicarPlanosMercadoLivre` (cobre Novo/Usado/Publicar): BLOQUEIA se faltar medida OU passar do máx absoluto (lado>200 / soma>300 / peso>50kg → ML recusa); AVISA se passar do Correios (lado>100/soma>200/peso>30) ou lado>70 (taxa extra). Dica ao vivo em `#dim-ml-aviso` embaixo das medidas (oninput). Cache-busters `?v=2` em dx-mercadolivre/shopee. Limites confirmados por web (ML help deu 403). TESTADO no navegador (vazio→bloqueia, 25x60x6/2kg→ok, 110→avisa, 210/55kg→bloqueia). FALTA: deploy.
- **Shopee respeita checkbox da Pág.2 (parcial)**: `publicarShopeeReal` agora PULA a variação que estiver desmarcada nas DUAS lojas (tc-sh1-{id}-on E tc-sh2-{id}-on). Limitação: backend espalha 1 chamada p/ todas as lojas autorizadas (`/integracoes/shopee/publicar` sem param de loja) → NÃO dá p/ publicar numa loja e não na outra pela tela (precisaria mudar backend). Testado (desmarcar var 3 → publica:false). OLX/Site NÃO ligados (decisão da usuária: OLX por API bloqueada).
- **Editor de foto + estratégia v5 (lote de ajustes)**: (Pág1) tirada a coluna ESQUERDA de edição — escondida (`#ferramentas-editor display:none`, preserva IDs p/ JS não quebrar); só **Importar (Sistema/PC) + Mover + Remover fundo + Salvar + Limpar** numa **barra fina no topo** (no h3); editor virou 1 coluna (canvas 62vh, maior). (Pág2) **Shopee reativada** (`TC_SHOW_SHOPEE=true`) 5×2; **Site=1, OLX=1** (param `limite` no `bloco()` do dx-titulos-conta.js) → **Resumo TOTAL 22**. Rodapé pág2: **Só cadastrar / Publicar USADO / Publicar NOVO** (ligados a `cadastrarPeca`/`publicarTudoUsado`/`publicarTudoNovo` que já existiam; ids `btn-publicar-usado/novo`). `dx-titulos-conta.js?v=2` (cache-buster). VERIFICADO no navegador. **GAP HONESTO (não corrigido — mexe no motor que publica):** a tela mostra 22, mas o motor real cria ML 10 (5×2, respeita checkbox tc-ml) + Shopee 10 (5×2 via backend, IGNORA checkbox tc-sh) + Site só via `publicarTudo` (pub-site/pub-olx ESTÃO DESMARCADOS no DOM oculto → hoje publicaria ~20) + OLX por API BLOQUEADA. Botões Novo/Usado NÃO publicam Site. Alinhar 22 real = wire do motor + teste de publicar de verdade. FALTA: testar fluxos reais + deploy `netlify deploy --prod --no-build --dir=.`.
- **Compatibilidade na tela de anúncio reorganizada (v5)**: (1) removida a compatibilidade DUPLICADA de cima (`compat-tags` abaixo da busca); (2) painel de **fotos dos carros** (`#panel-silhuetas`) saiu da lateral do canvas e foi pra uma **faixa horizontal ABAIXO do editor de fotos**; (3) editor de fotos passou de 3 → 2 colunas (ferramentas + canvas), canvas 86vh→54vh pra caber o painel; (4) lista de compatibilidade (texto) MANTIDA na lateral (coluna 3). CSS `.cars-panel` virou row-wrap; `.car-card` width 150px. NÃO toca na lógica de publicar (`compatData` segue a fonte da verdade). VERIFICADO no navegador (1500px): layout OK, sem erro de JS (só 404 de /ping). FALTA: usuária testar com peça real (carros aparecem) e deploy `netlify deploy --prod --no-build --dir=.`.
- **v5 PROMOVIDA PRA PRODUÇÃO (2026-06-05)**: criar-anuncio-v5.html no ar. Usuária confirmou o modelo das 2 páginas (Pág1 Dados+Fotos+Compat / Pág2 Estratégia de Publicação: ML Geisa, ML Domínio, Site DP, OLX — 5 variações cada, Resumo "Total 20" + Publicar). 19 links repontados de v3 → v5 (sistema/estoque/crm/desmonte-x, Python replace UTF-8). Backups `.bak-v3` ao lado de cada arquivo (reverter = trocar links de volta). VERIFICADO: parâmetros embed/sku/editar/duplicar tratados pelo core compartilhado (dx-criar-core.js); IDs dos campos da v5 batem (phPreencherFormulario preenche nome/oem/marca/modelo/preço OK); sidebar no embed = mesmo comportamento do v3 (não regrediu). **DESCOBERTA DEPLOY**: o erro "Failed retrieving extensions / fetch failed" é a etapa de BUILD — resolver com `netlify deploy --prod --no-build --dir=.` (site é HTML puro, não precisa build). NÃO precisou unlink/relink. Site = charming-marzipan-900fcf (dominiodaspecascatonho.com.br). FALTA: usuária testar fluxos reais no ar (catálogo, editar ?sku, embed no CRM, publicar de verdade).
- **Publicação ML religada** (5 títulos por conta, preço por desconto, checkbox) + **publica de verdade** (testado: MLB6905142982/MLB6905145830, conta default, usado).
- **Bug "Validation error" resolvido na raiz**: era VEHICLE_TYPE. Backend consulta a categoria: valor FIXO não envia; lista usa value_id.
- **Categoria de CARRO automática**: predizer prefere VEHICLE_TYPE Carro/Caminhonete (limpador: MLB457680/agrícola → MLB63512/carro). EM PRODUÇÃO.
- **Foto editada (base64) publica** (sobe pro /pictures/items/upload). DESCOBERTA: ML **apara borda branca** e limita ~1200px — quadrado branco é inútil (ML desfaz). Foto vai no limite da origem (~1200), ML centraliza nos cards. Frontend manda até 8 fotos.
- **Card do estoque reflete publicação**: publish agora grava em ml_anuncios (SKU base, on_conflict ml_id,conta, service key). VERIFICADO: 109045 virou "Publicado". (Antes /anuncios-db não tinha anúncios criados pelo nosso sistema.)
- **Buscar no Catálogo ML** (igual PartsHub, bloco dx-catalogo.js + endpoint /integracoes/mercadolivre/catalogo): por número/OEM/nome → auto-preenche nome, marca, modelo (do nome), OEM, categoria ML+nome, posição lado/eixo. "Importar fotos" opcional. Tema escuro do painel. TESTADO (1002256240 → MLB22645 Faróis Traseiros, Direita+Traseira).
- **Compatibilidade na descrição**: confirmado que entra (APLICAÇÃO/COMPATIBILIDADE/OBSERVAÇÕES via montarDescricaoWRX); corrigido bug que repetia o veículo ~15x (dedup de anos+grupos).
- **Shopee escondida** no painel de títulos (reversível: TC_SHOW_SHOPEE=true em dx-titulos-conta.js). ML5 desconto 20%→12%.
- **v3 PROMOVIDA PRA PRODUÇÃO (2026-06-05)**: deploy --prod do netlify-deploy; criar-anuncio-v3.html no ar; 20 links do painel (estoque/crm/sistema/desmonte-x/categoria-peca.js) repontados de criar-anuncio.html → criar-anuncio-v3.html (Python replace, UTF-8). criar-anuncio.html mantido como FALLBACK (reverter = trocar links de volta). Verificado: v3 200 + botão Buscar no Catálogo + estoque aponta pro v3. Domínio dominiodaspecas.com.br ainda com SSL preso → usar charming-marzipan-900fcf.netlify.app. FALTA: usuária testar fluxos em produção (catálogo, importar estoque, publicar, editar ?sku, embed crm).

## ✅ Feito nesta sessão (2026-06-04)
- Painel CRM voltou (era cache); criar-anúncio responsivo (cards não passam sob rodapé, 2 col mobile) + `_headers` no-cache.
- Disparo pedidos: 1 por número enquanto tiver pedido aberto.
- Anti-venda-dupla: sync leve horário (`sync_estoque_qtd.py`+Action), webhook ML instantâneo, fix pausa Shopee (status abnormal), 0 anúncios de vendidas ativos.
