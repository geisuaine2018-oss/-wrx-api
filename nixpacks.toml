[phases.setup]
nixPkgs = ["chromium", "nss", "nspr", "atk", "cups", "libdrm", "libxkbcommon", "gtk3", "pango", "cairo", "glib", "dbus"]

[phases.build]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python api_server.py"
